sudo: false
language: python
python:
  - "2.7"
install:
  - pip install python-coveralls
  - pip install tox-travis
  - pip install lambda-uploader
script: tox
after_success:
  - find ./ -name *coverage
  - coveralls
  - lambda-uploader
